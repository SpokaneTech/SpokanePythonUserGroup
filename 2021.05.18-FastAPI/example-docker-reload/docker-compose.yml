version: "3.9"
services:
  api:
    image: tiangolo/uvicorn-gunicorn-fastapi:3.8
    command: /start-reload.sh
    environment:
      - MAX_WORKERS=1
    volumes:
      - ./app:/app
    ports:
      - "8000:80"
